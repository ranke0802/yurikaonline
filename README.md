# Yurika Online (유리카 온라인)
> **Service Link:** [https://yurika-online.web.app/](https://yurika-online.web.app/)

<!-- AUTO_VERSION_BLOCK_START -->
## Build Metadata
- 배포 버전: **0.02.111**
- 마지막 버전 갱신: 2026-07-24
<!-- AUTO_VERSION_BLOCK_END -->

Yurika Online은 HTML5 Canvas와 Firebase를 기반으로 만든 실시간 웹 MMORPG입니다. 로그인, 캐릭터 성장, 튜토리얼, 멀티플레이어 동기화, 전투 UI를 한 프로젝트 안에서 다루고 있습니다.

현재 버전: **0.02.111**

## 핵심 기능

- 실시간 멀티플레이어: Firebase Realtime Database 기반 위치, 상태, 전투 동기화
- 전투 시스템: 체인 라이트닝, 매직 미사일, 파이어볼, 앱솔루트 베리어
- 성장 시스템: 레벨, 스탯, 스킬 강화, 인벤토리, 퀘스트 보상
- 온보딩: 프롤로그와 단계형 기본 튜토리얼
- 반응형 UI: PC, 모바일 세로, 모바일 가로 레이아웃 분기
- 배포 안정화: 서비스 워커와 버전 기반 캐시 무효화

## 최근 업데이트

### 0.02.032

- 미니맵을 눌러 레벨 제한과 권장 레벨, 출현 몬스터, 보스 전용 무기를 확인하고 이동할 수 있는 월드 지도 UI를 추가했습니다.
- 안개빛 호수, 뇌광 숲, 별그늘 유적과 각 필드의 일반 몬스터·보스를 추가하고, 원본 v2 스프라이트 시트의 셀 규격과 프레임 수를 그대로 사용하는 애니메이션 경로를 연결했습니다.
- 필드 보스별 보장 지팡이와 고유 스킬 변형, 강화·저장 호환, 과레벨 경험치 감소, 인벤토리가 가득 찼을 때의 보상함 보관을 추가했습니다.
- 맵 이동과 호스트 교체 중 몬스터·드롭·보스 재등장 상태가 중복되거나 유실되지 않도록 필드 단위 인계와 보상 중복 방지를 보강했습니다.

### 0.02.028

- 모바일 브라우저별 터치 판정 차이 때문에 일부 기기에서 미니맵 뒤에 남아 있던 가로 전환/닫기 버튼 두 개를, 터치 가로모드 기준으로 확실하게 숨기도록 정리했습니다.

### 0.02.027

- 새 서비스 워커가 활성화된 직후 한 번 더 버전 재로딩을 강제해, 버전 숫자는 올라갔는데도 첫 진입에서 오래된 JS 모듈 UI가 남아 있던 캐시 버스팅 누락을 보강했습니다.

### 0.02.026

- 모바일 가로 모드 우측 상단 미니맵 뒤에 남아 있던 세로 전환/닫기 버튼 두 개를 제거해, 동작하지 않는 컨트롤이 겹쳐 보이던 문제를 정리했습니다.

### 0.02.025

- 월드 플레이 중 브라우저 뒤로가기를 누르면 바로 빠져나가지 않고 `게임을 종료하시겠습니까?` 확인/취소 모달을 먼저 띄운 뒤, 확인 시 현재 상태를 저장하고 캐릭터 선택으로 돌아가도록 정리했습니다.

### 0.02.024

- 함께하기 파티 패널의 초기 위치와 레이어 우선순위를 다시 맞춰, 모바일 세로/가로에서 퀘스트·채팅·미니맵 같은 HUD에 가리지 않고 항상 위에서 드래그로 위치를 조정할 수 있게 정리했습니다.

### 0.02.023

- 모바일 인벤토리 상세보기를 인벤토리 팝업 내부 레이어에서 분리해 최상단 오버레이로 띄우도록 바꿨습니다. 이제 상세보기가 인벤토리 그리드·헤더·푸터에 가리지 않고 전체 내용을 안정적으로 볼 수 있습니다.

### 0.02.022

- 파티 패널의 종료 버튼으로 함께하기를 끝낼 때 바로 종료하지 않고, `함께하기를 종료하시겠습니까?` 확인/취소 모달을 먼저 띄우도록 정리했습니다.

### 0.02.021

- 튜토리얼 상태창 단계에서 상태창 전체가 계속 보이도록 하이라이트 예외 범위를 정리해, 내부 빈 영역과 검은 분할선처럼 보이던 dim 잔상이 생기지 않게 맞췄습니다.
- 튜토리얼의 파이어볼과 앱솔루트 베리어 사용 단계에 1초 완료 지연을 추가해, 스킬 타격/발동 모션을 확인한 뒤 다음 단계로 넘어가도록 정리했습니다.

### 0.02.020

- 친구 채팅 선물 수령 확인 모달의 메시지를 HTML 문자열 의존 없이 일반 텍스트로 정리해, PC 모드에서 `<strong>`, `<br>` 같은 태그가 그대로 노출되던 문제를 다시 막았습니다.

### 0.02.017

- 싱글 필드에서 인벤토리, 스킬창, 상태창, 친구창 같은 팝업을 열면 오토 공격을 포함한 플레이어 전투와 몬스터 생성·이동·공격·투사체 업데이트가 모두 완전히 멈추도록 월드 시뮬레이션 pause를 강화했습니다.
- 몬스터가 돌진/조준/시전 상태일 때 팝업을 열어도 해당 상태와 남은 타이머를 그대로 얼려 두고, 팝업을 닫으면 남은 시간만큼 이어서 재개되도록 modal pause 동작을 일반화했습니다.

### 0.02.016

- 함께하기(shared field) 중 인벤토리, 스킬창, 친구창 같은 팝업을 열어도 로컬 modal pause가 몬스터 생성·이동·전투 판정 전체를 멈추지 않도록 분리했습니다.
- 싱글 필드에서는 기존처럼 팝업을 열면 안전하게 몬스터 전투가 멈추는 동작을 그대로 유지합니다.

### 0.02.015

- 최소화한 친구 채팅창을 닫은 뒤 친구 목록에서 다시 채팅을 열면 이전 최소화 상태를 끌고 오지 않고, 일반 채팅창으로 새로 시작되도록 복구했습니다.
- 게임 인벤토리 같은 일반 팝업이 열린 동안에는 최소화 친구 채팅이 위에 남아 상세보기나 클릭을 방해하지 않도록 레이어 우선순위를 정리했습니다.

### 0.02.014

- 친구 채팅에서 선물 수령 완료 모달이 `<strong>`, `<br>` 태그를 문자열 그대로 노출하던 문제를 수정해, PC와 모바일 모두 아이템명 강조와 줄바꿈이 정상 표시되도록 정리했습니다.

### 0.02.002

- 튜토리얼 하이라이트가 팝업 본문과 상세 설명을 지나치게 가리지 않도록 `avoidTargets`와 런타임 포커스 해석을 보강했고, dim 경계 틈과 체인 라이트닝/매직 미사일/파이어볼/앱솔루트 베리어/인벤토리 단계의 어두움 문제를 함께 정리했습니다.
- 친구/채팅 UI에서 UID 직접 노출을 제거했고, compact/minimize 상태의 위치 복원 흐름을 다듬었으며, 선물은 첫 클릭 상세 확인 후 두 번째 클릭에 선택되도록 바꿨습니다. 수량 아이템은 추가 확인 모달을 거치고, 선물 수령 성공 시 획득 확인 모달이 뜹니다.
- 친구 목록 접속 상태 갱신을 더 안정적으로 묶어 온라인/오프라인 깜빡임을 줄였고, PC 친구 리스트 hover 흔들림과 모바일 수량 입력 기본값 문제를 함께 수정했습니다.
- 슬라임과 분열 슬라임은 이제 일반 공격 없이 돌진만 사용하고, 돌진 피해는 10으로 고정됩니다. 슬라임 계열 마석 드롭은 15~30 범위로 낮췄습니다.

### 0.02.001

- PC 인벤토리 팝업 그리드가 6칸으로 회귀하던 CSS 오버라이드를 10칸으로 되돌려 데스크톱 슬롯 밀도와 정렬을 복구했습니다.

### 0.02.000

- 모바일 전체화면 UX를 정리해 전체화면 상태에서 가로/세로 전환과 나가기 버튼을 분리하고, iPhone 상단 노치 영역에서도 주요 UI와 닫기 버튼이 가리지 않도록 safe area 보정을 적용했습니다.
- 모바일 공격 UI와 메뉴 묶음의 기본 투명도를 다른 UI 톤에 맞춰 조정해 첫 진입 시 전투 화면을 덜 가리도록 개선했습니다.

### 0.01.153

- 상태창 최종 레이아웃을 다시 정리해 PC, 모바일 가로, 모바일 세로에서 헤더와 하단 닫기 영역이 과하게 공간을 차지하지 않도록 압축했고, 핵심 스탯과 파생 수치가 내부 스크롤 없이 한 화면에 보이도록 복구했습니다.
- 상태창 튜토리얼의 모바일 세로 가이드 배치를 `popup-header-strip` 기준으로 조정해 수치 미리보기와 저장 단계에서 하이라이트 대상과 파생 수치가 함께 보이도록 개선했습니다.
- 무기 강화 전역 메시지를 강화 직후 바로 표시하고 1초만 노출되도록 바꿔 템포를 빠르게 맞췄습니다.
- iPhone 계열에서 강화 시 버벅거리던 구간을 줄이기 위해 강화 중간의 중복 UI 갱신을 없애고, 저전력 모바일 모드에서는 강화 연출의 글로우/펄스/추가 티어 이펙트를 더 가볍게 처리하도록 최적화했습니다.

### 0.01.107

- 진행 상태 저장 경로를 전체 프로필 저장과 경량 patch 저장으로 분리해, 경험치/골드/HP/퀘스트 카운트 같은 자주 바뀌는 값은 `users/{uid}/profile` 하위 필드만 갱신하도록 최적화했습니다.
- 싱글 기준 heartbeat 간격을 더 완화하고, shared field가 아닐 때는 `presence/{uid}/ts` fallback 갱신을 줄여 잔잔한 RTDB write와 rules evaluation을 낮추도록 조정했습니다.
- 매직미사일/파이어볼 난사 시 활성 투사체 수에 따라 trail, 파티클, spark 수를 자동으로 줄이는 동적 이펙트 스케일을 추가해 로컬 성능과 발열 부담을 낮추는 1차 최적화를 적용했습니다.

### 0.01.106

- 인벤토리를 열어 둔 상태에서 무기 강화 결과 플로팅 텍스트가 팝업 뒤에 가려지던 문제를 수정하기 위해, 중앙 메시지를 화면 기준 `fixed` 오버레이로 올려 강화 결과 문구가 항상 최상단에서 보이도록 조정했습니다.

### 0.01.105

- 미니맵의 어두운 반투명 배경은 유지하되, 적과 보스 점 크기는 이전 값으로 되돌려 화면을 가릴 정도로 커 보이던 표시를 완화했습니다.

### 0.01.104

- 나중에 접속한 3번째 게스트가 기존 호스트/게스트를 못 보거나, 새 접속 이후 기존 유저 이름이 `Unknown`으로 떨어지던 멀티 표시 문제를 줄이기 위해 same-field peer 감지 시 `users/{uid}` 실데이터를 1회 hydration하는 보강 경로를 추가했습니다.
- 원격 플레이어 이름은 이제 `profile -> 위치 패킷 이름 -> presence 이름` 순서로 더 안전하게 복원해서, 초기 동기화 순서가 꼬여도 기존 이름이 `Unknown`으로 쉽게 덮이지 않도록 정리했습니다.
- 게스트가 +8, +9 강화 장비를 착용했을 때 상대 화면에서만 장비 오라가 빠지던 문제를 수정하기 위해, 원격 플레이어 장비 데이터를 `itemData` 기준으로 정규화한 뒤 오라 렌더링에 넘기도록 통일했습니다.

### 0.01.103

- 오토 모드 중 파이어볼 조준선이 체인 라이트닝 자동 시전에 끊기지 않도록 조준 우선순위를 정리해, 홀드 조준 후 손을 떼는 흐름이 안정적으로 이어지도록 보정했습니다.
- 체인 라이트닝은 오토/수동 채널링 중 모두 이동 가능 상태를 유지하되 이동 속도를 기존 대비 40%로 맞춰, 사용 중 위치 조정은 가능하지만 지나치게 빠르게 끌고 다니지 않도록 정리했습니다.
- 게스트가 대왕 슬라임을 처치할 때 다음 반복 퀘스트 `슬라임 30마리 처치` 카운트가 미리 누적되던 문제를 막고, 보스 처치 시 반복 카운트를 즉시 초기화해 바로 재소환되지 않도록 수정했습니다.
- PvP에서는 부활 보호 상태를 원격에도 동기화하고 보호 중 대상은 공격 판정에서 제외해, 부활 직후 한쪽 화면에서만 죽거나 피해가 엇갈리던 문제를 줄였습니다.
- 원격 플레이어는 정지 패킷을 받으면 즉시 스냅되도록 바꿔 이동 후 멈출 때 앞뒤로 휘청이던 보간 현상을 줄였고, 파티 상태창도 로컬/원격 HP 감소와 회복을 바로 반영하도록 갱신 경로를 보강했습니다.
- `/p` 파티 목록창과 적대 대상 목록창을 가이드창처럼 마우스/터치 드래그로 이동할 수 있게 했고, 인벤토리에서 `무기 강화석` 또는 `축복받은 무기 강화석` 상세를 열었을 때 나오는 강화 버튼은 가운데 정렬되도록 정리했습니다.
- 무기 강화와 축복 강화 결과는 이제 화면 가운데 플로팅 텍스트로도 보여 주며, 성공/유지/파괴와 축복 +1/+2 결과에 맞춰 강화 단계와 무기 이름이 포함된 전용 문구가 출력되도록 보강했습니다.
- 미니맵 배경은 완전 투명 대신 살짝 어두운 반투명으로 바꾸고, 몬스터 도트를 키워 가독성을 높였으며 보스 몬스터는 체급에 비례해 더 크게 표시되도록 조정했습니다.
- `plans/test_cases_v0.01.101_v0.01.102_2026-04-07.md` 문서를 추가해 0.01.101 / 0.01.102 멀티 안정화 검증 케이스를 한글 체크리스트로 정리했습니다.

### 0.01.88

- 무기 상세 옵션 표기에서 강화 보너스를 별도 줄로 빼지 않고, `기본 수치%+강화 보너스%` 형태로 한 줄에 바로 붙여 보이도록 정리했습니다. 예를 들어 `별빛 매직 미사일 피해 45%+10%`처럼 표시되고, 뒤의 `+10%`는 녹색으로 강조됩니다.
- 인벤토리 무기 상세뿐 아니라 스킬 상세의 `현재 장착 무기 보정` 구간도 같은 표기 규칙으로 맞춰서, 괄호형 `강화 보너스` 문구 대신 실제 옵션 수치 뒤에 강화 증가분이 바로 붙어 보이도록 통일했습니다.
- 별빛, 푸른 불꽃, 붉은 섬광처럼 `%`로 보이는 확률/비율 계열 옵션을 볼 때 기본 수치와 강화 증가분을 한눈에 구분할 수 있게 되어, 강화 효율을 확인하기 더 쉬워졌습니다.

### 0.01.85

- `푸른 불꽃의 마력의 지팡이` 옵션을 기존의 파이어볼 피해 증가/처치 폭발형 효과에서, `푸른 파이어볼 연속 폭발 확률`과 `연속 폭발 데미지` 옵션으로 전면 교체했습니다.
- 푸른 파이어볼은 폭발 시 최대 50% 확률로 같은 위치에서 한 번 더 폭발하고, 추가 폭발은 기본 파이어볼 피해의 30%~80%를 사용합니다. 추가 폭발도 다시 같은 확률로 이어질 수 있어 운이 좋으면 연속으로 여러 번 터집니다.
- 기존에 이미 생성되어 있던 푸른 불꽃 지팡이도 바로 새 옵션으로 작동하도록, 예전 `rolledValues`를 새 연속 폭발 옵션으로 읽어주는 호환 처리도 같이 넣었습니다.
- 무기 상세와 스킬 상세의 설명 문구도 새 옵션 기준으로 정리해, 더 이상 `파이어볼 피해 증가`나 `처치 시 주변 폭발`로 보이지 않도록 맞췄습니다.

### 0.01.83

- 멀티 필드에서도 루트 프로필 저장이 전투 중 과도하게 연속 호출되지 않도록, 일반 `saveState()`의 debounce를 shared 기준으로 `350ms -> 800ms`로 조정했습니다. 월드 즉시 동기화가 필요한 `syncToWorld=true` 저장은 그대로 즉시 처리되므로, 전투 실시간성이나 필드 동기화 흐름은 유지됩니다.
- 일반 `profile_save` 백업은 shared 상태에서 `1분 -> 2분` 간격으로 완화해, 멀티 플레이 도중 백업이 너무 자주 쌓이며 내려받기 비용을 만드는 구간을 더 줄였습니다.
- solo-lite에서 이미 줄여둔 프로필 저장/백업 최적화를 멀티 쪽에도 안전한 범위로 확장한 패치라서, 기능적인 판정이나 멀티 전투 규칙은 건드리지 않고 RTDB 사용량만 추가 절감하는 방향으로 반영했습니다.

### 0.01.79

- Fixed an issue where normal monsters could stop spawning after the tutorial finished and field participation resumed.
- When host promotion happens slightly later, the world now reloads zone spawn rules on `hostChanged` and primes the spawn cycle again.

### 0.01.63

- 스탯 저장 튜토리얼에서 가이드 카드가 오른쪽 미리보기 패널을 가리지 않도록 `피해야 할 영역` 규칙을 추가하고, 상태창 파생 스탯 패널을 금지영역으로 지정했습니다.
- 상태창 닫기 단계는 이제 오른쪽 위 `X` 버튼과 하단 `닫기` 버튼을 동시에 빨간 박스로 강조해서, 어떤 닫기 버튼을 눌러도 바로 인지할 수 있게 맞췄습니다.
- 튜토리얼 엔진에 단계별 `avoidTargets` 지원을 넣어서, 앞으로도 주요 정보 패널을 가리지 않도록 단계별 배치 제어를 더 정확하게 할 수 있게 했습니다.

### 0.01.62

- 인벤토리에서 무기 강화석 또는 축복받은 무기 강화석을 선택하면 해당 강화석 슬롯이 테두리로 또렷하게 강조되고, 강화 대상 무기 강조는 그대로 유지되도록 정리했습니다.
- 무기 강화는 더 이상 상세 모달 안에서 연속으로 열리지 않고, 인벤토리에서 대상 무기만 바로 강화되는 흐름으로 바꿔 조작이 덜 번잡하게 이어지도록 했습니다.
- 강화 시작 공통 점멸, 성공 번쩍임, 유지 시 서서히 사그라드는 빛, 실패 균열 연출과 각각의 효과음을 추가했고, +7 녹색 / +8 파랑 / +9 붉은색 / +10 황금색 단계별 강화 성공 연출도 따로 넣었습니다.
- 최대 강화(+10) 무기를 다시 강화하려고 하면 `해당 무기는 이미 최종 강화된 상태입니다` 안내 모달이 먼저 뜨도록 막았습니다.

### 0.01.61

- 공격 버튼과 액션 버튼에 눌림 상태 연출을 강화해, 터치하거나 클릭했을 때 실제로 눌렸다는 피드백이 더 분명하게 보이도록 다듬었습니다.
- 튜토리얼 상태창 추천 스탯을 `VIT` 대신 `INT` 기준으로 통일하고, PC / 모바일 세로 / 모바일 가로 모두 지능 행을 직접 강조하도록 수정했습니다.
- 스킬 팝업 안 튜토리얼 가이드가 아이콘을 가리던 문제를 줄이기 위해 모바일 가로에서는 팝업 바깥 배치를 우선하게 하고, 모바일 세로에서는 인라인 가이드 카드 높이와 줄바꿈 규칙을 조정해 잘림을 완화했습니다.
- 스킬 상세 모달과 스킬 팝업을 다시 열 때 내부 스크롤이 이전 위치를 유지하던 문제를 정리해, 다른 스킬을 볼 때도 항상 맨 위에서 시작하도록 맞췄습니다.
- 첫 대왕 슬라임 처치 전까지 잡은 슬라임 수가 반복 퀘스트에 섞여 들어가던 문제를 고쳐, 반복 퀘스트 카운트가 첫 대왕 슬라임 처치 이후부터만 시작되도록 보정했습니다.

### 0.01.60

- 모바일 세로 모드에서 스킬 팝업과 상세 모달을 설명할 때 튜토리얼 가이드 카드가 핵심 UI를 가리지 않도록, 팝업 내부 상단/하단 띠 영역을 우선 사용하는 배치 규칙과 더 작은 인라인 카드 스타일을 적용했습니다.
- 스킬 상세, 인벤토리 상세, 확인 모달 같은 오버레이도 튜토리얼 배치 계산에서 동일한 활성 팝업으로 인식하도록 보강해 세로 튜토리얼의 겹침 문제를 줄였습니다.
- 매직 미사일의 목표 좌표를 몬스터 기준 정중앙으로 바로잡아 우하단으로 빗나가던 문제를 수정했고, 미사일 충돌 반경을 넓혀 넉백이 섞여도 적중률이 너무 쉽게 무너지지 않도록 보정했습니다.
- 원격 플레이어 매직 미사일 연출도 같은 중심 좌표 기준으로 맞춰, 멀티플레이 상황에서도 시각적으로 어색하게 보이지 않도록 정리했습니다.

### 0.01.59

- 튜토리얼의 스킬 설명 단계는 이제 hover 툴팁이 아니라 `상세 모달 열기 -> 상세 모달 닫기` 순서로 진행되도록 다시 묶어서, 설명을 보는 도중 다음 스텝으로 튀는 문제를 줄였습니다.
- 체인 라이트닝, 매직 미사일, 파이어볼, 앱솔루트 베리어 모두 설명 확인 뒤에 `설명 닫기` 단계를 명시적으로 추가해 PC / 모바일 세로 / 모바일 가로에서 단계가 더 안정적으로 이어지도록 정리했습니다.
- 모바일 파이어볼 튜토리얼에는 `버튼을 누른 채 드래그해서 조준 -> 손을 떼어 발사` 흐름을 별도 단계로 넣어, 실제 입력 방식과 튜토리얼이 일치하도록 맞췄습니다.
- 튜토리얼의 레이아웃별 `onStart` 액션 해석을 보완해서, 모바일 전용 몬스터 스폰과 팝업 정리 같은 준비 동작도 각 기기 레이아웃에서 올바르게 적용되도록 수정했습니다.
- 튜토리얼 가이드 위치는 활성 팝업 내부 요소를 설명할 때 전체 팝업과의 충돌을 더 신경 쓰도록 보정했고, 스킬 팝업 단계는 Playwright로 PC / 모바일 세로 / 모바일 가로 흐름을 직접 검증했습니다.

### 0.01.57

- 튜토리얼 가이드가 더 이상 화면 중앙을 크게 가리지 않도록, 단계별로 `top-card / left-card / bottom-sheet / floating-compact` 배치를 고르는 presentation 구조를 넣었습니다.
- 튜토리얼 하이라이트는 단순 빨간 박스 하나에서 `ring / frame / spotlight / callout` 방식으로 확장해, PC / 모바일 세로 / 모바일 가로에서 실제 눌러야 하는 영역을 더 자연스럽게 강조하도록 바꿨습니다.
- `basic_training` 주요 단계에 `questText / focus / presentation` 메타를 추가해, 퀘스트 패널 문구와 튜토리얼 카드가 각 기기 레이아웃에 맞게 다르게 동작하도록 정리했습니다.
- 매직 미사일은 시전 시점에 한 번 고정하는 방식이 아니라, 각 미사일이 실제 발사되는 순간의 대상 위치를 다시 잡아 그 지점까지만 유도 비행하도록 바꿔 적중률을 보완했습니다.
- 무기 상세 모달의 `강화 / 축복 강화` 버튼을 제거하고, `무기 강화석 / 축복받은 무기 강화석`을 눌러 강화 대상을 선택하는 흐름으로 정리해 인벤토리 강화 UX를 더 단순하게 만들었습니다.
- 퀘스트 보상 알림은 헤더와 카드에 동시에 뜨지 않도록 정리하고, 실제 보상 수령 카드 쪽에서만 인지할 수 있게 정돈했습니다.

### 0.01.56

- 모바일 가로 모드에서 상태창/스킬 아이콘의 알림 점이 각 아이콘 우측 상단에 정확히 붙도록 메뉴 버튼 기준점을 다시 정리했습니다.
- 퀘스트 보상 수령 카드에도 우측 상단 빨간 점을 추가해, 퀘스트 헤더뿐 아니라 실제 수령 위치에서도 즉시 인지할 수 있게 했습니다.
- 모바일 가로 모드에서 퀘스트 완료 보상 카드가 PC와 같은 녹색 톤으로 보이도록 claim 상태 스타일 우선순위를 다시 맞췄습니다.
- 스킬 클릭 시 열리는 상세 설명 모달은 본문, 요약 카드, 섹션 텍스트까지 전부 왼쪽 정렬로 정리해 가독성을 높였습니다.
- 튜토리얼 구조 개선을 위한 별도 설계 문서 [`plans/tutorial_optimization_plan_2026-04-02.md`](plans/tutorial_optimization_plan_2026-04-02.md) 를 추가했습니다.

### 0.01.54

- 일반공격 번개 사운드를 더 전기적인 치지직 느낌으로 바꾸고, 연쇄 전격도 가벼운 팝음 대신 잔류 스파크가 튀는 느낌으로 재구성했습니다.
- 파이어볼은 발사와 폭발 모두 저음과 충격감을 더해서 기존보다 더 묵직한 `쿠쾅` 계열의 타격감으로 조정했습니다.
- 매직 미사일은 벨소리 같은 가벼운 캐스팅음을 걷어내고, 연속 발사와 적중에 더 `팍파파팍`한 타격감이 나도록 발사/히트 음을 다시 설계했습니다.

### 0.01.53

- 모바일에서 이동 조이스틱과 파이어볼 버튼을 동시에 누른 상태에서도, 파이어볼 조준은 버튼을 누른 손가락만 따라가고 조이스틱 터치에는 영향받지 않도록 멀티터치 식별을 보정했습니다.

### 0.01.52

- 몬스터가 무기 강화석을 드랍할 때 테스트용으로 20% 확률로 `축복받은 무기 강화석`이 나오도록 추가했습니다.
- 인벤토리 상세에 `축복 강화`를 별도로 넣고, 축복 강화석 사용 시 성공 50%, 실패 시 강화 단계 유지, 성공 시 `+1~2` 증가하는 강화 규칙을 연결했습니다.
- 축복 강화석은 인벤토리에서 은은한 금빛 배경과 광채가 보이도록 표현을 추가했습니다.
- 상태창과 상단 초상화에 기본값으로 깔려 있던 마법사 스프라이트 배경을 제거해, 실제 초상화가 로드될 때만 캐릭터 이미지가 보이도록 정리했습니다.
- 모바일 파이어볼은 이동 조이스틱과 분리해서, 버튼을 누른 순간 현재 바라보는 방향으로 조준선이 생기고 누른 손가락의 드래그 각도로만 조준이 바뀌도록 보정했습니다.

### 0.01.51

- 스킬 상세보기 튜토리얼 단계가 hover 툴팁만 기다리던 문제를 보완해서, 스킬 상세 모달을 열어도 같은 단계 완료로 정상 진행되도록 수정했습니다.

### 0.01.50

- 모바일에서는 파이어볼 버튼을 누른 손가락으로만 조준이 바뀌도록 입력을 분리하고, 이동 조이스틱이나 방향 입력은 파이어볼 조준에 영향을 주지 않도록 정리했습니다.
- 모바일 파이어볼 조준은 터치 시작 순간의 바라보는 방향을 초기값으로 잡고, 손가락을 움직일 때만 조준선과 발사 지점이 갱신되도록 맞췄습니다.
- PC 모드에서는 기존처럼 파이어볼 입력 시 즉시 발사되는 흐름을 유지하도록 되돌렸습니다.

### 0.01.49

- 파이어볼 조준선이 홀드 중 이동 입력에도 반응하도록 보정해서, 방향키나 조이스틱으로 바라보는 방향을 바꾸면 조준선도 같이 회전하도록 맞췄습니다.
- 파이어볼 조준 시작 직후 기본 방향은 항상 현재 바라보는 방향 기준으로 잡히도록 유지하고, 드래그 조준이 없을 때는 이동 방향 변화가 그대로 초기 조준 방향을 갱신하도록 정리했습니다.

### 0.01.48

- 파이어볼 조준 중에는 스킬 버튼을 누른 채 손가락이나 포인터를 움직여 조준 위치를 계속 바꿀 수 있게 했습니다.
- 드래그 좌표를 월드 좌표로 환산해 조준 가이드와 실제 발사 지점에 반영하도록 연결해서, 모바일에서도 더 자연스럽게 조준 후 발사할 수 있게 했습니다.

### 0.01.47

- 파이어볼을 즉시 발사형에서 `누르면 조준 가이드 표시 -> 놓으면 발사` 흐름으로 바꿔서, 발사 전에 실제 사거리와 폭발 범위를 눈으로 확인할 수 있게 했습니다.
- 조준 가이드는 현재 바라보는 방향을 기준으로 파이어볼 진행 폭과 최대 사거리, 끝 지점 폭발 반경까지 함께 보여주도록 넣었습니다.
- 원격 플레이어가 사용하는 파이어볼도 같은 목표 지점을 기준으로 시각화되도록 맞춰서 멀티플레이 화면에서도 투사체 방향이 덜 어색하게 보이도록 정리했습니다.

### 0.01.46

- 자동 일반공격이 임시로 잡은 타겟까지 수동 타겟처럼 빨간 원 마커를 띄우던 문제를 정리했습니다.
- 자동 타겟과 수동/튜토리얼 타겟을 분리해서, 자동공격은 정상 동작하되 어색한 빨간 타겟 표시만 더 이상 나오지 않도록 개선했습니다.

### 0.01.45

- 상태창에서 개발자 모드를 켰을 때의 동작을 다시 이전 흐름으로 맞춰, 검은 배경의 녹색 개발자 HUD가 월드 화면에 뜨고 상태창에서는 초기화 버튼만 토글되도록 복구했습니다.
- 플레이 화면 개발자 HUD에서 이름→UID 검색 UI는 제거해, 개발자 수치만 보이는 원래 형태에 가깝게 정리했습니다.
- `[Auto]` 버튼은 유지하되 미니맵 왼쪽 아래 위치를 유지하도록 배치만 정리했습니다.

### 0.01.44

- 상태창 개발자 모드는 이전 방식으로 다시 돌려, 개발자 모드일 때 상태창에서는 초기화 버튼만 보이도록 복구했습니다.
- 이름→UID 검색 UI는 플레이 화면 개발자 오버레이로 되돌렸고, 미니맵 왼쪽의 투명 HUD 안에서 다시 사용할 수 있게 정리했습니다.
- `[Auto]` 버튼은 미니맵의 왼쪽 아래 쪽으로 다시 옮겨, 공격 버튼과 상단 우측 아이콘 영역을 가리지 않도록 조정했습니다.

### 0.01.43

- 상태창에 잠깐 들어갔던 개발자용 검색/표시 UI는 원복해, 일반 플레이 상태창에서는 개발자 요소가 보이지 않도록 정리했습니다.
- 투명 개발자 HUD는 다시 월드 화면 전용으로 유지하면서, 미니맵 바로 왼쪽에 붙어 나오도록 위치를 조정했습니다.
- `[Auto]` 버튼은 공격 버튼과 상단 상태/가방 아이콘을 가리지 않도록 우측 HUD 사이의 빈 공간으로 다시 이동시켰습니다.

### 0.01.42

- 개발자 모드 오버레이를 맵 왼쪽의 얇은 텍스트 HUD로 줄여서, 켜 둬도 전투 화면을 크게 가리지 않도록 정리했습니다.
- 개발자용 이름→UID 검색은 더 이상 오버레이에 두지 않고 상태창 안에서만 보이게 옮겼고, 개발자 모드가 켜진 상태에서만 노출되도록 정리했습니다.
- 자동 일반공격은 사거리 안의 유효 타겟을 스스로 다시 잡아 실제로 동작하도록 보완했고, 토글 버튼은 `[Auto]` 고정 문구에 `비활성 회색 / 활성 녹색`으로 바꾸고 공격 버튼을 가리지 않는 위치로 재배치했습니다.
- 공격속도는 최대 `2.0`으로 고정했고, 체인 라이트닝 일반공격 틱은 기존 대비 약 15% 느리게 조정했습니다. 상태창 파생 공격속도와 스킬 상세 모달의 틱 간격 계산도 같은 규칙으로 맞췄습니다.

### 0.01.41

- 모바일 최적화 프로필에서 전투 이펙트 저하 플래그를 분리해, HUD/미니맵/네트워크 최적화는 유지하면서 스킬 모션과 전투 연출은 다시 기존 품질에 가깝게 복구했습니다.
- 기존에는 모바일 저전력 모드가 켜지면 `useReducedEffects`도 함께 활성화되어 체인 라이트닝, 투사체, 마법진, 보호막 연출이 과하게 단순화되고 있었는데, 이제는 전투 이펙트는 기본적으로 축소하지 않도록 정리했습니다.
- 최근 적용한 blur 제거, 미니맵 dirty redraw, 패킷 최적화는 그대로 유지돼 전투 연출만 되살리고 나머지 발열/네트워크 개선은 계속 가져가도록 맞췄습니다.
- 이번 조정 내용은 [`plans/mobile_thermal_packet_optimization_report_2026-04-01_effect_quality_recovery.md`](plans/mobile_thermal_packet_optimization_report_2026-04-01_effect_quality_recovery.md)에 별도 메모로 정리했습니다.

### 0.01.40

- 모바일 저전력 모드에서 미니맵, 미니맵 메뉴, 인벤토리 상세 오버레이, 확인/사망 모달에 남아 있던 `backdrop-filter` 경로를 더 정리해 합성 비용을 줄였습니다.
- 미니맵에 상태 기반 dirty redraw 캐시를 추가해, 플레이어/원격 플레이어/몬스터 배치가 눈에 띄게 바뀌지 않았을 때는 실제 캔버스 redraw를 건너뛰도록 개선했습니다.
- 미니맵 캔버스가 교체되거나 해상도가 바뀐 경우에는 캐시를 무시하고 다시 그리도록 보완해, 화면 전환 후 빈 미니맵이 남지 않게 했습니다.
- 인코딩 깨짐을 다시 점검해 조이스틱 발바닥 아이콘 문자열이 깨져 있던 `index.html` 한 곳을 정상 복구했습니다.
- 이번 UI 경량화와 인코딩 점검 내용은 [`plans/mobile_thermal_packet_optimization_report_2026-04-01_phase7_ui_polish.md`](plans/mobile_thermal_packet_optimization_report_2026-04-01_phase7_ui_polish.md)에 별도 보고서로 정리했습니다.

### 0.01.39

- 플레이어 zone 동기화에서 루트 `users` `child_changed` 의존을 걷어내고, 위치·presence·프로필·전투 상태를 유저별 하위 필드 리스너로 분리해 hot path 수신 부담을 줄였습니다.
- 원격 플레이어별 zone user cache를 추가해, 프로필과 위치가 다른 타이밍으로 도착하더라도 버퍼를 조합해 더 안정적으로 생성·복원되도록 정리했습니다.
- 프로필-only 변경도 `playerUpdate` 이벤트로 흘려 보내도록 보강해 이름, 레벨, 방어력, 안전 상태, 장비, 파티, 적대 관계가 원격 플레이어에 더 안정적으로 반영되게 했습니다.
- `RemotePlayer`가 `defense`, `isPaused`까지 즉시 반영하도록 보강해 프로필-only 업데이트 시 실제 월드 표현과 내부 상태가 더 잘 맞도록 했습니다.
- 이번 플레이어 hot path 분리 작업은 [`plans/mobile_thermal_packet_optimization_report_2026-04-01_phase1_hot_path.md`](plans/mobile_thermal_packet_optimization_report_2026-04-01_phase1_hot_path.md)에 별도 보고서로 정리했습니다.

### 0.01.38

- 몬스터 동기화를 우선순위 기반 cadence로 재설계해, 보스·전투 중·피격 직후 몬스터는 빠르게 유지하고 멀리 떨어진 idle 몬스터는 더 느리게 전송하도록 조정했습니다.
- 로컬/원격 플레이어 주변까지 함께 보는 AOI(관심 영역) 판단을 추가해, 호스트 화면 밖이더라도 실제 전투 중인 몬스터는 더 자주 동기화되도록 보완했습니다.
- 몬스터 스폰, 보스 스폰, 강제 sync, 주요 상태 변화는 queued write를 기다리지 않고 즉시 flush 하도록 바꿔 체감 지연 없이 네트워크 write만 줄이도록 정리했습니다.
- 몬스터 activity timestamp와 full sync 메타 보존을 추가해, 최근 전투가 있었던 몬스터와 보스 메타 정보가 게스트 쪽에서도 더 안정적으로 반영되게 했습니다.
- 이번 몬스터 동기화 최적화 내용은 [`plans/mobile_thermal_packet_optimization_report_2026-04-01_phase4.md`](plans/mobile_thermal_packet_optimization_report_2026-04-01_phase4.md)에 별도 보고서로 정리했습니다.

### 0.01.36

- 모바일 성능 telemetry를 추가해 update/render 시간, RTDB write 수, 추정 전송량, HUD 및 미니맵 갱신 빈도를 개발 오버레이에서 바로 확인할 수 있게 했습니다.
- 플레이어 presence와 zone 동기화 경로를 정리해 heartbeat와 zone profile payload를 더 가볍게 만들고, profile 저장/동기화 write를 집계 가능한 구조로 정리했습니다.
- 몬스터 상태 전송을 batch update 기반으로 묶고, 원격 플레이어는 오프스크린일 때 저주기 업데이트를 사용하도록 바꿔 모바일 CPU 사용량과 패킷 수를 함께 줄였습니다.
- HUD cooldown 갱신 빈도와 개발 오버레이 갱신 빈도를 모바일 cadence에 맞게 조정하고, 미니맵/HUD/원격 플레이어 갱신 횟수도 telemetry에 포함했습니다.
- 서비스워커를 app shell / static asset 분리 전략으로 재구성해 문서는 network-first, 정적 자산은 cache-first로 처리하도록 바꿨습니다.

### 0.01.35

- 모바일 전반에 공통 thermal profile을 적용해 렌더/업데이트 FPS와 DPR을 기기 환경에 맞게 낮추고, 과도한 발열을 줄이는 1차 최적화를 반영했습니다.
- 게임 루프에 update FPS 제어와 backlog 컷오프를 추가해 프레임이 밀릴 때 CPU가 과도하게 따라잡기를 시도하지 않도록 조정했습니다.
- 이동 패킷과 heartbeat 간격을 적응형으로 바꾸고, 최근 네트워크 송신이 있으면 중복 heartbeat를 생략하도록 조정해 패킷 사용량을 줄였습니다.
- 플레이어 profile 저장에 debounce/flush 흐름을 도입해 짧은 시간에 연속으로 발생하는 전체 저장 요청을 병합하도록 개선했습니다.
- 모바일 성능 모드에서는 몬스터 동기화 주기와 위치 변경 임계값을 완화해 호스트 write 빈도를 낮추도록 조정했습니다.

### 0.01.22

- 세로모드에서 상태창, 스킬창, 인벤토리창이 함께 보이던 팝업 숨김 우선순위 문제를 수정했습니다.
- 인벤토리 UX 개선 방향을 PC, 모바일 세로, 모바일 가로 기준으로 정리한 설계서를 추가했습니다.

### 0.01.21

- 모바일 인벤토리 상단의 큰 장착 무기/골드 카드를 제거하고, 슬롯판 중심 구조로 다시 정리했습니다.
- 골드와 장착 무기를 일반 슬롯형 유틸 칸으로 통합해 화면 점유를 줄였습니다.
- 모바일 세로/가로에서 인벤토리 스크롤 영역 계산을 다시 맞추고, 착용 장비는 노란 테두리로 바로 보이게 바꿨습니다.

### 0.01.20

- 인벤토리를 300칸 기준으로 확장하고, 하단에 현재 사용 슬롯 수를 `n/300` 형식으로 표시하도록 정리했습니다.
- 인벤토리 우측 고정 상세 패널을 제거하고, 클릭 또는 터치 시 상세 모달이 뜨는 구조로 바꿨습니다.
- PC, 모바일 세로, 모바일 가로 각각에 맞는 인벤토리 레이아웃을 따로 구성해 슬롯 밀도와 상세 모달 형태를 분리했습니다.

### 0.01.16

- 대왕 슬라임 등장곡을 빠른 전투형에서 느리고 음침한 질감의 JSON BGM으로 다시 구성했습니다.
- `rich_string`, `warp_bass`, `fm_bell`, `harp` 조합으로 부드럽지만 불길한 분위기를 강화했습니다.
- 기존처럼 절대적으로 시끄러운 보스전 BGM이 아니라, 등장 순간의 공포감이 오래 남도록 템포와 타격감을 낮췄습니다.

### 0.01.15

- 모바일 발열 완화를 위해 모바일 DPR 제한, 렌더 FPS 캡, HUD 및 미니맵 갱신 빈도 최적화를 적용했습니다.
- PWA 아이콘을 다시 프레이밍하고 `apple-touch-icon`까지 추가해 설치 아이콘 가장자리 품질을 정리했습니다.
- 파티 초대/응답 모달 흐름, 파티 패널 편의성, 파티 보상 공유 규칙을 보강했습니다.

### 0.01.03

- 모바일 세로/가로 레이아웃 충돌을 분리 정리했습니다.
- 회전 이후 튜토리얼 가이드와 하이라이트 위치를 다시 계산하도록 보강했습니다.
- HUD와 팝업 배치를 안전 영역 기준으로 다시 조정했습니다.

### 0.01.02

- 모바일 가로 HUD를 전용 레이아웃으로 재배치했습니다.
- 튜토리얼 문구를 `desktop`, `mobile-portrait`, `mobile-landscape`로 분기했습니다.
- 튜토리얼 하이라이트 박스와 반응형 안내 문구를 추가했습니다.

### 0.01.01

- 튜토리얼 스킬 툴팁 설명을 강화했습니다.
- 인벤토리 닫기 단계와 팝업 이벤트 흐름을 안정화했습니다.
- 버전 자동화 훅을 추가해 커밋 시 버전 정보를 함께 갱신하도록 했습니다.

## 실행 방법

이 프로젝트는 정적 웹 서버로 실행하면 됩니다. `live-server` 기준으로 점검했고, Windows PowerShell에서 `npx live-server --port=8080`처럼 실행하면 환경에 따라 `listen EACCES: permission denied 0.0.0.0:8080`가 날 수 있습니다.

원인:

- `8080` 포트가 로컬 환경에서 막혀 있거나 다른 정책에 걸릴 수 있습니다.
- `live-server`가 기본적으로 `0.0.0.0`에 바인딩하려고 해서, Windows 환경에서 더 까다롭게 막히는 경우가 있습니다.

권장 기준:

- 같은 PC에서만 실행할 때는 `127.0.0.1`
- 기본 포트는 `8081` 또는 `5500`
- 가장 쉬운 방법은 `npm start`

### Windows PowerShell

프로젝트 루트에서:

```powershell
npm start
```

브라우저에서 접속:

```text
http://127.0.0.1:8081
```

수동 실행이 필요하면:

```powershell
npx -y live-server . --host=127.0.0.1 --port=8081 --no-browser
```

`8081`이 이미 사용 중이면:

```powershell
npx -y live-server . --host=127.0.0.1 --port=5500 --no-browser
```

### Windows CMD

프로젝트 루트에서:

```cmd
npm start
```

수동 실행:

```cmd
npx -y live-server . --host=127.0.0.1 --port=8081 --no-browser
```

### macOS / Linux

프로젝트 루트에서:

```bash
npm start
```

수동 실행:

```bash
npx -y live-server . --host=127.0.0.1 --port=8081 --no-browser
```

포트를 바꾸고 싶다면:

```bash
npx -y live-server . --host=127.0.0.1 --port=5500 --no-browser
```

### WSL

WSL에서도 같은 방식으로 실행하면 됩니다.

```bash
npm start
```

또는:

```bash
npx -y live-server . --host=127.0.0.1 --port=8081 --no-browser
```

Windows 브라우저에서는 보통 아래 주소로 접속하면 됩니다.

```text
http://127.0.0.1:8081
```

### 실기기 확인

휴대폰이나 태블릿에서 확인할 때는 로컬 LAN 서버 대신 Firebase 배포 주소를 사용합니다.

- 로컬 실행 방법은 개발 PC에서 브라우저로 확인하는 용도입니다.
- 모바일 실기기 확인은 배포 후 서비스 링크로 접속하는 흐름을 기준으로 합니다.
- 이 README에서는 휴대폰용 로컬 서버 실행 방법은 안내하지 않습니다.

## 버전 관리

- `version.txt`를 단일 버전 기준으로 사용합니다.
- 커밋 전 `.githooks/pre-commit`에서 `scripts/sync-version.js`가 실행됩니다.
- 이 스크립트는 `version.txt`, `src/js/main.js`, `src/js/world/scenes/LoginScene.js`, `index.html`, `sw.js`, `README.md`를 같은 버전으로 맞춥니다.
- `index.html`의 서비스 워커, CSS, 메인 모듈 쿼리스트링도 같은 버전으로 동기화합니다.

## 개발 메모

- 로컬 로그 파일 `live-server.log`, `live-server.err.log`는 커밋 대상이 아닙니다.
- 배포 후 캐시가 남아 있으면 `version.txt` 기준으로 새로고침이 유도됩니다.
- 사용자에게 보이는 문자열은 UTF-8 기준으로 유지해야 합니다.

## 라이선스

© 2026 Yurika Online. All rights reserved.
